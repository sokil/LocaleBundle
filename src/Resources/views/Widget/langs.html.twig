<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            {{ app.request.locale|upper }} <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" role="menu">
            {% for language in locales_provider.getLanguages() %}
                <li><a href="{{ path("set_locale", {locale: language.getAlpha2()}) }}?back={{ app.request.requestUri }}">
                        {{ language.getLocalName()|capitalize }}
                    </a></li>
            {% endfor %}
        </ul>
    </li>
</ul>
